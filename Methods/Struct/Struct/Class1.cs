using System;

namespace StructAssignment
{
    // Create a struct called Number with the property "Amount" of data type decimal
    public struct Number
    {
        public decimal Amount { get; set; }
    }
}